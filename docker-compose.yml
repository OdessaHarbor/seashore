version: '3.7'

services:
  converter:
    build: .
    command: >
      gunicorn -b 0.0.0.0:5000
        --access-logfile -
        "Converter_api.app:create_converter()"
    environment:
      PYTHONUNBUFFERED: 'true'
    volumes:
      - '.:/Converter_api'
    ports:
      - '5000:5000'
